{
  "name": "@compass/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "effect-tsgo diagnostics --project ./tsconfig.json --format json",
    "test": "vp test run --passWithNoTests"
  },
  "dependencies": {
    "@compass/contracts": "workspace:*",
    "@effect/ai-openrouter": "catalog:",
    "@effect/platform-node": "catalog:",
    "@effect/sql-sqlite-node": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:"
  }
}
